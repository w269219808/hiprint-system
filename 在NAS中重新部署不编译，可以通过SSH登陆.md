# 在NAS中重新部署编译，可以通过SSH登陆

---

## ✅ 这个方法的好处

| 对比           | 绿联图形界面“重新部署”     | SSH 命令行 `docker-compose build --no-cache` |
| -------------- | -------------------------- | -------------------------------------------- |
| 是否重新编译   | 不一定（看 yaml 有没有变） | ✅ **100% 会重新编译**                        |
| 是否使用缓存   | 可能使用缓存               | `--no-cache` 强制不用缓存                    |
| 能看到详细日志 | 可以（在日志界面）         | ✅ 终端实时输出，更直观                       |
| 操作速度       | 较慢（点来点去）           | ✅ 快（一条命令）                             |

---

## 🚀 SSH 操作步骤（完整版）

### 1️⃣ SSH 登录 NAS
```bash
ssh 你的NAS用户名@你的NASIP
# 例如：ssh wuzhongming@192.168.110.107
# 然后输入密码
```

### 2️⃣ 进入项目目录
```bash
cd /volume1/文件临时中转站/hiprint/hiprint
# 绿联的具体路径可能不同，根据你的实际情况调整

# 2. 强制重新构建（用 sudo）
sudo docker compose build --no-cache

# 3. 启动容器（用 sudo）
sudo docker compose up -d

# 4. 查看日志（用 sudo）
sudo docker compose logs -f

```

如果不确定路径，可以在绿联文件管理器里找到项目文件夹，右键查看“属性”或“路径”，复制过来。





### 4️⃣ 查看日志确认启动成功

```bash
docker-compose logs -f
```
按 `Ctrl+C` 退出日志查看。

---

## 💡 以后更新代码的两种方式任选

| 方式           | 操作                                      | 适用场景                   |
| -------------- | ----------------------------------------- | -------------------------- |
| **图形界面**   | 点“清除” → 点“重新部署”                   | 不想用命令行，习惯图形操作 |
| **SSH 命令行** | `docker-compose up -d --build --no-cache` | 追求速度，想看详细构建日志 |

两种方式效果一样，选你顺手的就行。

---

## ⚠️ 注意事项

1. **确保在项目目录执行**：`docker-compose` 命令需要在 `docker-compose.yaml` 文件所在的目录执行。
2. **如果提示找不到 `docker-compose` 命令**：绿联的 Docker 可能用的是 `docker compose`（有空格），试试：
   ```bash
   docker compose up -d --build --no-cache
   ```
3. **如果 SSH 提示权限不足**：命令前加 `sudo`：
   ```bash
   sudo docker-compose up -d --build --no-cache
   ```

---

## 🧪 你可以先试试

先用 SSH 方式部署一次，体验一下命令行带来的直接反馈。如果觉得好用，以后就两种方式结合着用——**快速迭代时用命令行，日常查看状态时用图形界面**。

试完之后告诉我结果，如果遇到什么报错，把错误信息贴出来我帮你看看！